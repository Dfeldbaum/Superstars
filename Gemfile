source 'https://rubygems.org'

gem 'sinatra'
gem 'json'
gem 'pry'
gem 'mysql2'
gem 'sinatra-activerecord'


# run bundle in terminal, then install rake (like gulp), mysql2, sinatra-activerecord
# bundle again

group :development do
 gem 'mysql2'
end

group :production do
  gem 'mysql2'
end


#bundle again
#Rake -T to see built in rake tasks
#rake db:create_migration NAME=happy_places 